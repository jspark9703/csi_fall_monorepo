[core]
    remote = local-storage
    autostage = true
[remote "local-storage"]
    url = .dvc/cache
    # 로컬 저장소 설정:
    # - URL은 상대경로 또는 절대경로 모두 가능
    # - 상대경로: .dvc/cache (모노레포 루트 기준)
    # - 절대경로: C:\path\to\dvc-cache (Windows) 또는 /path/to/dvc-cache (Unix)
