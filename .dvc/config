[core]
    remote = local-storage
    autostage = true

[remote "local-storage"]
    url = .dvc/cache
    # 로컬 스토리지 설정
    # 캐시 위치: .dvc/cache/ (모노레포 루트 기준 상대경로)
    # 절대경로 변경 가능: /path/to/cache 또는 C:\path\to\cache
